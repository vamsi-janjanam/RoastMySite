# Optional. When set, roasts are generated by the Claude API (claude-opus-4-8).
# When absent, the app falls back to a deterministic mock roast engine so it
# still runs fully offline.
ANTHROPIC_API_KEY=
